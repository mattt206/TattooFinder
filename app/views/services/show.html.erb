<div class="container">
  <div class = "row">
    <div class = "col-12">

        <div class="card">

          <div class="card-body" style="background-color:grey;">
            <h2 ><%= @service.style %></h2>
            <%#= image_tag "unknow.png"%>
            <% if @service.photo.attached? %>
              <%= cl_image_tag @service.photo.key, class: "card-img-top img-fluid" %>
            <% else %>
              <%= image_tag "unknow.png"%>
            <% end %>
            <h4> Precio base: <%= @service.price %></h4>
            <p> Descripción: <%= @service.description %><p>
            <h4> Artista: <%= @service.user.nickname  %> </h4>
              <div>
              <%#= link_to "Back", services_path, class: "btn btn-outline-dark"%>
              <%#= link_to "Artist profile", profile_path(@user.id), class: "btn btn-outline-light"%>
                <%= link_to "Back", services_path, class: "btn btn-outline-dark"%>
                <%= link_to "reservar", services_path, class: "btn btn-outline-dark"%>
                <%#= link_to "Edit", edit_service_path(@service), class: "btn btn-primary mx-2" %>
                    <%#= link_to "Delete", service_path(@service), class: "btn btn-warning mx-2", data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
              </div>
          </div>
        </div>

    </div>
  </div>
</div>
